interface:
  display_name: "APXM Compile And Execute"
  short_description: "Validate, compile, and run APXM AIR workflows"
  default_prompt: "Use $apxm-compile-and-execute to validate and run this APXM AIR workflow."
policy:
  allow_implicit_invocation: true
